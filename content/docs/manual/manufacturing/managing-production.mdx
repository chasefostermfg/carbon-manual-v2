---
title: "Managing production"
description: "Schedule, track and plan work across the shop."
---

One job is easy to watch. Dozens at once is the real job. This page shows how Carbon turns customer demand into a plan, tells you what to **make** and what to **buy**, and shows you whether the shop is on track.

## Where this lives in Carbon

This is the **Production** module. Open it from the home screen, then use **Planning**, **Jobs**, and the dashboard inside it.

The dashboard is your shop at a glance: **Active Jobs**, **Work Center Utilization**, and which machines are busy. Acme runs work through machines like the **DMU 50**, the **Inspection Station**, and its **Burn Tables**.

<AppShot
  src="/screenshots/manual/production-dashboard.png"
  alt="The Production module dashboard with active jobs and work center utilization"
  caption="Production module home. Active Jobs and Work Center Utilization (DMU 50, Inspection Station, Burn Tables) tell you what's running and where it's piling up."
/>

## Step 1: Demand in, plan out

Carbon's planning compares two things:

- **Demand** — what's needed: open sales orders, materials jobs will consume, safety stock.
- **Supply** — what covers it: stock on hand, open purchase orders, jobs in progress.

Where demand beats supply, Carbon raises a **suggestion**. For a **Buy** item, that's a [purchase order](/manual/buying/purchasing-basics). For a **Make** item, it's a [job](/manual/manufacturing/jobs).

<Mermaid
  chart={`flowchart LR
  D[Demand] --> P[Planning]
  S[Supply] --> P
  P --> Buy[Buy item shortage]
  P --> Make[Make item shortage]
  Buy --> PO[Purchase Order]
  Make --> J[Job]`}
/>

Here's a real example. Sales order [SO000069](/manual/selling/sales-orders) needs **500** Counterweight Hooks. Each hook uses **40 LB** of A36 steel plate, so that's **20,000 LB** of steel. Acme doesn't have that on the shelf, so Carbon suggests buying it — which became [PO000057 to Ryerson](/manual/buying/purchasing-basics). The hook itself is a **Make** part, so the order became job **J000097**.

<Callout type="tip">
  **Make vs Buy** is one setting on each [item](/manual/items/defining-an-item). The hook is *Make*; its steel is *Buy*. That flag decides whether a shortage becomes a job or a PO. See [Replenishment](/manual/items/replenishment).
</Callout>

## Step 2: Review suggestions in Planning

Open **Production → Planning**. You get one row per item that needs attention — what's required, what's available, the shortage, and the suggested action.

<AppShot title="Planning" url="app.carbon.ms/x/production/planning" caption="Production → Planning. Shortages on the right become suggestions you review." flush>
  <DataTable
    columns={[
      { key: "item", label: "Item", mono: true },
      { key: "src", label: "Replenish" },
      { key: "demand", label: "Demand", align: "right" },
      { key: "supply", label: "Supply", align: "right" },
      { key: "short", label: "Short", align: "right" },
      { key: "suggest", label: "Suggestion" },
    ]}
    rows={[
      { item: "CWH-A36-15", src: <Tag>Make</Tag>, demand: "500 EA", supply: "0 EA", short: "500 EA", suggest: <StatusBadge tone="amber">Make → Job</StatusBadge> },
      { item: "A36-STEEL-PLT", src: <Tag>Buy</Tag>, demand: "20,000 LB", supply: "0 LB", short: "20,000 LB", suggest: <StatusBadge tone="blue">Buy → PO</StatusBadge> },
      { item: "INV-MNT-BRKT.D", src: <Tag>Make</Tag>, demand: "4,000 EA", supply: "0 EA", short: "4,000 EA", suggest: <StatusBadge tone="amber">Make → Job</StatusBadge> },
      { item: "BBB-360-2-01", src: <Tag>Make</Tag>, demand: "1,000 EA", supply: "0 EA", short: "1,000 EA", suggest: <StatusBadge tone="green">Job exists</StatusBadge> },
    ]}
  />
</AppShot>

<Steps>

<Step>
**Set the horizon.** At the top, choose how far ahead to look. Carbon only raises suggestions for demand inside the horizon.
</Step>

<Step>
**Read each row.** When **Short** is above zero, Carbon proposes an action. 20,000 LB of steel with nothing on hand produces a *Buy → PO*.
</Step>

<Step>
**Adjust before you commit.** Change a quantity or pull a date earlier. Acme's PO000057 ended up at **54,001 LB** — the buyer rounded up to cover the next two Radiant orders.
</Step>

<Step>
**Convert what you accept.** *Buy* suggestions become draft [purchase orders](/manual/buying/purchasing-basics); *Make* suggestions become [jobs](/manual/manufacturing/jobs). Carbon links each one back to the demand that created it.
</Step>

</Steps>

<Callout type="warn">
  A suggestion is a proposal, never automatic. Carbon won't raise a PO or release a job on its own — you always confirm.
</Callout>

## Step 3: Schedule jobs across work centers

Every [operation](/manual/overview/glossary) in a job points at a **work center** — a machine or station like the **DMU 50** or a **Burn Table**. Each operation carries a time, and operations run **After Previous** (step 20 waits for step 10). That's how Carbon builds a schedule.

Job **J000097** ran three operations in sequence:

<AppShot title="Job J000097 — Operations" url="app.carbon.ms/x/job/J000097/operations" caption="Three operations, each pinned to a work center, run in order." flush>
  <DataTable
    columns={[
      { key: "seq", label: "Seq", align: "right" },
      { key: "op", label: "Operation" },
      { key: "wc", label: "Work center" },
      { key: "time", label: "Time", align: "right" },
      { key: "status", label: "Status", align: "right" },
    ]}
    rows={[
      { seq: "10", op: "Flame Cut", wc: "Burn Table — PA2", time: "45 min/pc", status: <StatusBadge tone="green">Done</StatusBadge> },
      { seq: "20", op: "Sandblasting", wc: "Sand Blasting Room", time: "2 min/pc", status: <StatusBadge tone="green">Done</StatusBadge> },
      { seq: "30", op: "Inspection", wc: "Inspection Station", time: "10 min/pc", status: <StatusBadge tone="green">Done</StatusBadge> },
    ]}
  />
</AppShot>

<Steps>

<Step>
**Find the bottleneck.** The dashboard's **Work Center Utilization** shows load per machine. Acme has three Burn Tables but only one Sand Blasting Room — so sandblasting is the constraint.
</Step>

<Step>
**Balance the load.** When a process has more than one machine, move work to a less-busy one. Flame Cut can run on Burn Table PA1, PA2, or the Texas table.
</Step>

<Step>
**Respect the sequence.** Operations run *After Previous*, so Carbon keeps them in order. You decide where the whole chain fits against other jobs.
</Step>

</Steps>

<Callout type="tip">
  Work-center rates also cost your jobs. Time booked on Burn Table PA2 absorbs labor at $50/hr and machine at $25/hr — so J000097's cost came from recorded shop time, not a guess.
</Callout>

## Step 4: Prioritize what runs first

When more work wants a machine than it can handle, **priority** and **due date** decide the order. Here's Acme's live Counterweight Hook backlog — three jobs for the same customer, stacked up:

<AppShot title="Jobs — Counterweight Hook backlog" url="app.carbon.ms/x/production/jobs?item=CWH-A36-15" caption="Three hook jobs in the queue, due in sequence." flush>
  <DataTable
    columns={[
      { key: "job", label: "Job", mono: true },
      { key: "so", label: "Sales order", mono: true },
      { key: "qty", label: "Qty", align: "right" },
      { key: "due", label: "Due date" },
      { key: "status", label: "Status", align: "right" },
    ]}
    rows={[
      { job: "J000097", so: "SO000069", qty: "500 EA", due: "2026-06-26", status: <StatusBadge tone="green">Completed</StatusBadge> },
      { job: "J000098", so: "SO000070", qty: "1,500 EA", due: "2026-07-01", status: <StatusBadge tone="amber">In Progress</StatusBadge> },
      { job: "J000099", so: "SO000071", qty: "3,000 EA", due: "2026-07-21", status: <StatusBadge tone="blue">Ready</StatusBadge> },
    ]}
  />
</AppShot>

Read it as a plan: J000097 finished first, J000098 is running now, J000099 is staged and *Ready*. Sequencing by due date keeps the single Sand Blasting Room from causing a missed deadline.

<Callout type="warn">
  A job must be **released** before the floor can touch it. Statuses go in order: *Draft → Planned → Ready → In Progress → Completed*. Releasing is on the [Jobs](/manual/manufacturing/jobs) page.
</Callout>

## Step 5: Track progress

The jobs list is your live status board. Completion updates itself as operators record finished pieces in the [shop floor app](/manual/manufacturing/shop-floor) — you never type a percentage by hand.

<StatGrid cols={4}>
  <Stat label="Open jobs" value="4" hint="not yet completed" />
  <Stat label="In progress" value="2" hint="J000095 · J000098" />
  <Stat label="Past due" value="1" hint="J000095 — needs attention" />
  <Stat label="Work centers" value="19" hint="across 2 locations" />
</StatGrid>

<AppShot title="Jobs" url="app.carbon.ms/x/production/jobs" caption="A live cross-section of the shop — done, running, queued." flush>
  <DataTable
    columns={[
      { key: "job", label: "Job", mono: true },
      { key: "item", label: "Item", mono: true },
      { key: "qty", label: "Qty", align: "right" },
      { key: "done", label: "Complete", align: "right" },
      { key: "due", label: "Due date" },
      { key: "status", label: "Status", align: "right" },
    ]}
    rows={[
      { job: "J000095", item: "BBB-360-2-01", qty: "1,000 EA", done: "in progress", due: "2026-04-23", status: <StatusBadge tone="amber">In Progress</StatusBadge> },
      { job: "J000096", item: "INV-MNT-BRKT.D", qty: "4,000 EA", done: "0", due: "2026-07-01", status: <StatusBadge tone="neutral">Planned</StatusBadge> },
      { job: "J000097", item: "CWH-A36-15", qty: "500 EA", done: "500", due: "2026-06-26", status: <StatusBadge tone="green">Completed</StatusBadge> },
      { job: "J000098", item: "CWH-A36-15", qty: "1,500 EA", done: "in progress", due: "2026-07-01", status: <StatusBadge tone="amber">In Progress</StatusBadge> },
      { job: "J000099", item: "CWH-A36-15", qty: "3,000 EA", done: "0", due: "2026-07-21", status: <StatusBadge tone="blue">Ready</StatusBadge> },
    ]}
  />
</AppShot>

What to read off this screen:

- **Completion is real.** J000097 shows 500 of 500 because operators clocked off all 500 pieces.
- **Status says where to look.** *In Progress* jobs are on the floor now — J000095 and J000098 get today's attention.
- **Due dates flag risk.** J000095 is due **2026-04-23** — already past — and still running. That's your cue to investigate.

<Callout type="info">
  **The loop.** Demand creates suggestions → you turn them into jobs and POs → you schedule jobs across work centers → operators run them → the dashboard updates in real time → new demand restarts it.
</Callout>

## Next steps

<Cards>
  <IconCard title="Jobs" href="/manual/manufacturing/jobs" description="Create, release and manage a single production job." icon={<Factory className="size-4" />} />
  <IconCard title="The shop floor (MES)" href="/manual/manufacturing/shop-floor" description="How operators clock on, record quantities and complete operations." icon={<Workflow className="size-4" />} />
  <IconCard title="Replenishment" href="/manual/items/replenishment" description="Set Make vs Buy and the rules that drive planning suggestions." icon={<Package className="size-4" />} />
</Cards>
